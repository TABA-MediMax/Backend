/*



@Service
public class ImageService {

    public String saveImage(byte[] imageBytes) {
        // 이미지 서버에 저장하고 경로를 반환하는 로직

        return imagePath;
    }

    public Long saveImagePathToDatabase(String imagePath) {
        // 이미지 경로를 DB에 저장하고 생성된 이미지 ID를 반환하는 로직

        return imageId;
    }
}



*/